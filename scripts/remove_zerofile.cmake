##############################################################################
# Скрипт удаления файла нулевой длины
# Аргумент FILENAME - путь/имя удаляемого файла
##############################################################################

if(EXISTS "${FILENAME}")
    file(SIZE "${FILENAME}" FILE_SIZE)
    if(FILE_SIZE EQUAL 0)
        file(REMOVE "${FILENAME}")
    endif()
endif()
